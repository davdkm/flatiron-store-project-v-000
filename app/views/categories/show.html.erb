<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
  <li><%= link_to item.title, item_path(item) %> | $<%= item.price.to_f/100 %>
    <%= form_tag url: "/carts", method: 'POST' do %>
      <%= hidden_field_tag :item_id, item.id %>
      <%= submit_tag 'Add to Cart' %>
    <% end %>
  </li>
<% end %>
</ul>
